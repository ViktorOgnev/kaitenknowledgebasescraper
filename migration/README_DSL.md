# Kaiten DSL - Infrastructure as Code для Kaiten

## Что это?

**Kaiten DSL** — декларативный язык (YAML) для описания конфигурации Kaiten. Позволяет:

- 📝 Описать всю инфраструктуру Kaiten в YAML файле
- 🚀 Применить конфигурацию одной командой
- 🔄 Версионировать конфигурацию в Git
- 🧪 Тестировать изменения в dry-run режиме
- 📦 Делиться шаблонами конфигураций

## Установка

```bash
cd migration/
pip3 install -r requirements.txt
```

## Быстрый старт

### 1. Создайте файл `config.yaml`:

```yaml
version: 1.0

kaiten:
  domain: mycompany
  token: ${env:KAITEN_TOKEN}

spaces:
  - name: Мой проект
    boards:
      - name: Задачи
        columns:
          - name: To Do
            type: queue
          - name: Done
            type: done
```

### 2. Установите переменную окружения:

```bash
export KAITEN_TOKEN="ваш-api-токен"
export KAITEN_DOMAIN="mycompany"
```

### 3. Примените конфигурацию:

```bash
python3 kaiten_dsl_engine.py apply config.yaml
```

## Команды

### `apply` — применить конфигурацию
```bash
python3 kaiten_dsl_engine.py apply config.yaml
```
Создаёт/обновляет все ресурсы в Kaiten согласно YAML файлу.

### `dry-run` — проверка без изменений
```bash
python3 kaiten_dsl_engine.py dry-run config.yaml
```
Показывает что будет сделано, но **не применяет** изменения.

### `validate` — валидация конфигурации
```bash
python3 kaiten_dsl_engine.py validate config.yaml
```
Проверяет синтаксис и структуру YAML файла.

---

## Структура DSL

### Базовая структура

```yaml
version: 1.0              # Версия DSL (обязательно)

kaiten:                   # Подключение к Kaiten
  domain: company
  token: ${env:KAITEN_TOKEN}

custom_properties: []     # Пользовательские поля
tags: []                  # Метки
card_types: []            # Типы карточек
spaces: []                # Пространства и доски
automations: []           # Автоматизации
cards: []                 # Карточки (опционально)
```

---

## Секции конфигурации

### 1. **kaiten** — подключение

```yaml
kaiten:
  domain: mycompany       # Домен Kaiten
  token: ${env:TOKEN}     # API токен (используйте переменные!)
```

**Переменные окружения:**
- `${env:VAR_NAME}` — подставляет значение из `ENV`

---

### 2. **custom_properties** — пользовательские поля

```yaml
custom_properties:
  # Текстовое поле
  - name: Комментарий
    type: string

  # Число
  - name: Бюджет
    type: number

  # Дата
  - name: Дедлайн
    type: date

  # Email
  - name: Email клиента
    type: email

  # Телефон
  - name: Телефон
    type: phone

  # Селект (выбор одного)
  - name: Приоритет
    type: select
    settings:
      options:
        - Низкий
        - Средний
        - Высокий

  # Мультиселект (выбор нескольких)
  - name: Теги
    type: multiselect
    settings:
      options:
        - Новое
        - Важное
        - Срочное

  # Справочник (для CRM)
  - name: Клиент
    type: catalog
    catalog_fields:
      - name: Название компании
        required: true
      - name: ИНН
      - name: Телефон

  # Чекбокс
  - name: Согласовано
    type: checkbox

  # Пользователь
  - name: Куратор
    type: user
```

**Поддерживаемые типы:**
- `string` / `text` — текст
- `number` — число
- `date` — дата
- `email` — email
- `phone` — телефон
- `select` — выбор одного значения
- `multiselect` — выбор нескольких
- `catalog` — справочник (CRM)
- `checkbox` — да/нет
- `user` — пользователь

---

### 3. **tags** — метки

```yaml
tags:
  - name: Срочно
    color: "#FF0000"

  - name: Важно
    color: "#FFA500"

  - name: B2B
    color: "#0066FF"
```

**Параметры:**
- `name` — название метки
- `color` — цвет в HEX формате

---

### 4. **card_types** — типы карточек

```yaml
card_types:
  - name: Лид
    color: "#FFD700"

  - name: Задача
    color: "#20B2AA"

  - name: Баг
    color: "#FF4500"
```

---

### 5. **spaces** — пространства и доски

```yaml
spaces:
  - name: CRM
    access: private         # private | public
    boards:
      - name: Воронка лидов
        description: Основная воронка продаж
        columns:
          - name: Новые
            type: queue
            wip_limit: 10   # Лимит WIP (опционально)

          - name: В работе
            type: in_progress

          - name: Готово
            type: done

        lanes:              # Дорожки (опционально)
          - name: Менеджер 1
          - name: Менеджер 2
```

**Типы колонок:**
- `queue` — очередь
- `in_progress` — в работе
- `done` — выполнено

---

### 6. **automations** — автоматизации

```yaml
automations:
  # Автоназначение ответственного
  - name: Автоназначение
    space: CRM
    trigger: card_moved_to
    conditions:
      column: В работе
    actions:
      - type: set_owner
        value: event_author

  # Добавление комментария через N дней
  - name: Напоминание
    space: CRM
    trigger: time_based
    conditions:
      days_in_column: 3
      column: Отправлено КП
    actions:
      - type: add_tag
        value: Срочно
      - type: add_comment
        value: "Прошло 3 дня, пора дожать!"

  # Перемещение по условию
  - name: Автоперемещение
    space: CRM
    trigger: field_changed
    conditions:
      field: Договор подписан
      value: true
    actions:
      - type: move_card
        column: Закрыто успешно
```

**Триггеры:**
- `card_moved_to` — карточка перемещена
- `card_created` — карточка создана
- `comment_added` — добавлен комментарий
- `field_changed` — изменено поле
- `time_based` — по времени

**Действия:**
- `set_owner` — назначить ответственного
- `add_tag` — добавить метку
- `add_comment` — добавить комментарий
- `move_card` — переместить карточку

---

### 7. **cards** — карточки (начальные данные)

```yaml
cards:
  - title: Тестовая карточка
    board: CRM.Воронка лидов      # Формат: Пространство.Доска
    column: Новые                  # Название колонки
    description: |
      Многострочное описание
      карточки
    properties:
      Клиент: ООО "Рога и копыта"
      Бюджет: 500000
      Телефон: "+7 916 123-45-67"
```

**Формат ссылок:**
- `board: "Пространство.Доска"`
- `column: "Название колонки"`

---

## Переменные

### Переменные окружения

```yaml
kaiten:
  token: ${env:KAITEN_TOKEN}
  domain: ${env:KAITEN_DOMAIN}
```

Используйте переменные для:
- ✅ API токенов (безопасность)
- ✅ Доменов (разные окружения)
- ✅ Любых чувствительных данных

---

## Примеры

### Пример 1: Минимальная конфигурация

```yaml
version: 1.0
kaiten:
  domain: ${env:KAITEN_DOMAIN}
  token: ${env:KAITEN_TOKEN}

spaces:
  - name: Проект
    boards:
      - name: Задачи
        columns:
          - name: To Do
            type: queue
          - name: Done
            type: done
```

### Пример 2: CRM с автоматизациями

См. файл: `examples/crm_basic.yaml`

### Пример 3: Полная конфигурация

См. файл: `examples/full_example.yaml`

---

## Use Cases

### 1. Версионирование конфигурации
```bash
git init
git add config.yaml
git commit -m "Initial Kaiten configuration"
```

### 2. Разные окружения (dev/prod)
```bash
# dev
export KAITEN_DOMAIN=dev-company
python3 kaiten_dsl_engine.py apply config.yaml

# prod
export KAITEN_DOMAIN=company
python3 kaiten_dsl_engine.py apply config.yaml
```

### 3. CI/CD интеграция
```yaml
# .github/workflows/kaiten.yml
name: Deploy Kaiten Config
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Apply Kaiten config
        env:
          KAITEN_TOKEN: ${{ secrets.KAITEN_TOKEN }}
          KAITEN_DOMAIN: company
        run: python3 kaiten_dsl_engine.py apply config.yaml
```

### 4. Шаблоны для команд
Создайте шаблоны конфигураций для разных команд:
- `templates/crm.yaml`
- `templates/dev-team.yaml`
- `templates/support.yaml`

---

## Порядок применения

DSL применяет изменения в правильном порядке с учётом зависимостей:

1. **custom_properties** — пользовательские поля
2. **tags** — метки
3. **card_types** — типы карточек
4. **spaces** → **boards** → **columns** → **lanes** — структура
5. **automations** — автоматизации
6. **cards** — карточки

---

## Ограничения

### Что можно создать через DSL:
✅ Пространства
✅ Доски
✅ Колонки и дорожки
✅ Пользовательские поля
✅ Метки
✅ Типы карточек
✅ Автоматизации
✅ Карточки

### Что НЕ поддерживается:
❌ Пользователи (создаются вручную)
❌ Права доступа (настраиваются в UI)
❌ Обновление существующих ресурсов (только создание)

---

## Best Practices

### 1. Используйте переменные окружения
```yaml
# ✅ Правильно
token: ${env:KAITEN_TOKEN}

# ❌ Неправильно (токен в Git!)
token: "296a5709-99bc-49a7-96e0-c0a1b236091f"
```

### 2. Разделяйте конфигурации
```
configs/
├── base.yaml           # Базовая структура
├── crm.yaml            # CRM настройки
└── automations.yaml    # Автоматизации
```

### 3. Тестируйте через dry-run
```bash
python3 kaiten_dsl_engine.py dry-run config.yaml
```

### 4. Документируйте конфигурацию
```yaml
# CRM воронка для отдела продаж
# Создана: 2025-12-04
# Владелец: Иван Петров
spaces:
  - name: CRM
    # ...
```

### 5. Версионируйте в Git
```bash
git add config.yaml
git commit -m "feat: добавлена автоматизация напоминаний"
```

---

## Troubleshooting

### Ошибка "401 Unauthorized"
Проверьте правильность токена:
```bash
echo $KAITEN_TOKEN
```

### Ошибка "Пространство не найдено"
Убедитесь что имена пространств совпадают с указанными в `spaces:[]`

### Dry-run не показывает изменения
Это нормально — dry-run использует mock данные для симуляции

### Автоматизация не применяется
Проверьте:
1. Имя пространства указано правильно
2. Пространство уже создано
3. Формат trigger и actions корректен

---

## Примеры из коробки

В папке `examples/` есть готовые конфигурации:

1. **simple.yaml** — минимальная конфигурация
2. **crm_basic.yaml** — базовая CRM с автоматизациями
3. **full_example.yaml** — полный пример всех возможностей

Запуск:
```bash
python3 kaiten_dsl_engine.py apply examples/crm_basic.yaml
```

---

## Расширение DSL

DSL легко расширяется. Добавьте свои обработчики в `KaitenDSLEngine`:

```python
def apply_custom_resource(self, resources: List[Dict]):
    for resource in resources:
        # Ваша логика
        pass
```

---

## Roadmap

Планируется добавить:
- [ ] Импорт существующей конфигурации из Kaiten
- [ ] Обновление ресурсов (сейчас только создание)
- [ ] Удаление ресурсов
- [ ] Шаблоны с параметрами (Jinja2)
- [ ] Валидация перед применением
- [ ] Rollback изменений
- [ ] Diff между конфигурациями

---

## Поддержка

Если возникли проблемы:
1. Проверьте синтаксис YAML
2. Запустите `validate` для проверки
3. Используйте `dry-run` для тестирования
4. Проверьте логи выполнения
